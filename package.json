{
  "name": "event-submit-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for submitting event suggestions to GitHub Issues",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
